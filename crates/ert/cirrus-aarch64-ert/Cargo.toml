[package]
name = "cirrus-aarch64-ert"
edition = "2024"
version.workspace = true
description = "Fail-closed AArch64 decoder and symbolic ERT facade"

[dependencies]
cirrus-ert-core = { version = "0.1.0", path = "../cirrus-ert-core" }
disarm64 = { version = "0.1.26", default-features = false, features = ["full"] }
